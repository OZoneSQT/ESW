#include <stdio.h> 
#include <stdlib.h>
#include "queue.h"
#include "test.h"


int main(void)
{
	int size = 3;

	// item itm;
	
	QueueInit(size);
//	printf("Init Queue to length: %d\n", &size);

	/*
	QueuePut(getItmA());


	QueuePut(getItmB());
	itm = QueueGet();
	printf("number = %d and string = %s\n", itm.number, itm.string);
	itm = QueueGet();
	printf("number = %d and string = %s\n", itm.number, itm.string);
	QueuePut(getItmC());
	QueuePut(getItmD());
	itm = QueueGet();
	printf("number = %d and string = %s\n", itm.number, itm.string);
	QueuePut(getItmE());
	itm = QueueGet();
	printf("number = %d and string = %s\n", itm.number, itm.string);
	itm = QueueGet();
	printf("number = %d and string = %s\n", itm.number, itm.string);  */


	return 0;
}